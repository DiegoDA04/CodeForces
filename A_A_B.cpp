#include<bits/stdc++.h>
#define ll long long
#define vi vector<int>

using namespace std; 

int main() {
    int t;
    cin >> t;
    while (t--)
    {
        string s;
        cin >> s;

        cout << (s[0] - '0') + (s[2] - '0') << endl;
    }
    

    return 0;
}